# Disable or uninstall

Disable the plugin without removing it:

```bash
openclaw plugins disable safe-agent-cli-mcp
```

Remove the installed or linked plugin:

```bash
openclaw plugins uninstall safe-agent-cli-mcp
```

Check the resulting state:

```bash
openclaw plugins list
openclaw plugins doctor
```
